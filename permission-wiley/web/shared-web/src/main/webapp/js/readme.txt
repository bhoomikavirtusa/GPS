smarkoff:
When tried upgrade from jQuery 1.7.2 to 1.8.3, also need to upgrade jQuery UI to at least 1.8.22
(because otherwise get "s.charCodeAt is not a function" javascript error).
But jQuery UI causes weird issues related to landing page dataTable -- an issues in our own javascript I'm sure.)
